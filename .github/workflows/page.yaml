name: Print Wiki Page Content

on:
  workflow_dispatch:

jobs:
  # print-wiki-content:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout Wiki
  #       run: |
  #         git clone https://github.com/${{ github.repository }}.wiki.git
  #         cd $(basename ${{ github.repository }}.wiki)
      
  #     - name: Print Wiki Page Content
  #       run: |
  #         cd $(basename ${{ github.repository }}.wiki)
  #         cat Home.md

  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Call reusable workflow
        uses: Brug-Org/action_automation/.github/workflows/update-wiki.yml@main
        with:
          ambiente: 'ti-lado-b'
          tag: 'release-1.34.5'

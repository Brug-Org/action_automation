name: Main Workflow

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  call-reusable-workflow:
    uses: oBrug-Org/action_automation/.github/workflows/reusable-workflow.yml@main
    with:
      prefix: 'TI'
